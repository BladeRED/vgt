{% block body %}
    <!doctype html>
    <html lang="fr">
    {% include 'parts/headadmin.twig' %}

    <body>


    {% include 'parts/headeradmin.twig' %}


    <main class=pb-5">
        <!--INSERT CODE HERE -->

        <section class="mb-5 pb-5">
            <div class="container mb-5 my-5 py-5">
                <table class="table table-dark table-striped" id="reviewsTable">
                    <thead>
                    <tr>
                        <th scope="col">ID</th>
                        <th scope="col" class="text-center">Note</th>
                        <th scope="col" class="text-center">Review</th>
                        <th scope="col" class="text-center">Date</th>
                        <th scope="col" class="text-center">Signalé?</th>
                        <th scope="col" class="text-center">Utilisateur</th>
                        <th scope="col" class="text-center">Actions</th>

                    </tr>
                    </thead>
                    <tbody>
                    {% for review in reviews %}
                        <tr>
                            <th scope="row">{{ review.id }}</th>
                            <td>{{ review.note }}</td>
                            <td>{{ review.comment }}</td>
                            <td>{{ review.date|format_datetime(locale='fr',pattern="dd MMMM YYYY") }}</td>
                            <td>{{ review.isSignaled }}</td>
                            <td>{{ review.gamer.pseudo }}</td>
                            <td>
                                <a href="/admin/deleteReviews/{{ review.id }}" type="button"
                                   class="btn btn-lg btn-danger my-3 ">Supprimer</a></td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        </section>
    </main>


    {% include 'parts/footer.twig' %}

    <!-- JS scripts -->

    {% include 'parts/JS_scripts.twig' %}

    <script src="https://code.jquery.com/jquery-3.6.0.slim.min.js"
            integrity="sha256-u7e5khyithlIdTpu22PHhENmPcRdFiHRjhAuHcs05RI=" crossorigin="anonymous"></script>
    <script type="text/javascript"
            src="https://cdn.datatables.net/v/bs5/dt-1.12.1/r-2.3.0/sc-2.0.6/sl-1.4.0/datatables.min.js"></script>

    <script>
        $(document).ready(function () {
            $('#reviewsTable').DataTable({

            });
        });
    </script>

    </body>
    </html>
{% endblock %}