{% block body %}
    <!doctype html>
    <html lang="fr">
    {% include 'parts/head.twig' %}

    <body>


    {% include 'parts/headeradmin.twig' %}


    <main>
        <!--INSERT CODE HERE -->

        <section>
            <div class="container my-5">
                <table class="table table-dark table-striped">
                    <thead>
                    <tr>
                        <th scope="col">ID</th>
                        <th scope="col" class="text-center">Pseudo</th>
                        <th scope="col" class="text-center">Mail</th>
                        <th scope="col" class="text-center">Role</th>
                        <th scope="col" class="text-center">Actions</th>

                    </tr>
                    </thead>
                    <tbody>
                    {% for user in users%}
                    <tr>
                        <th scope="row">{{user.id}}</th>
                        <td>{{user.pseudo}}</td>
                        <td>{{ user.mail }}</td>
                        <td>{{ user.role }}</td>
                        <td><a href="#" type="button" class="btn btn-lg btn-secondary">Modifier</a>
                            <a href="#" type="button" class="btn btn-lg btn-danger">Supprimer</a></td>
                    </tr>
                  {% endfor %}
                    </tbody>
                </table>
            </div>
        </section>
    </main>


    {% include 'parts/footer.twig' %}

    <!-- JS scripts -->

    {% include 'parts/JS_scripts.twig' %}

    <script>

    </script>

    </body>
    </html>
{% endblock %}